{% extends "base.html.twig" %}

{% block body %} 
    <h2> Votre Panier </h2>
    <table class="table">
        <tr>
            <th>N°de Commande</th>
            <th>Livre</th>
            <th>Titre du Livre</th>
            <th>Status</th>
        </tr>
    {%for cart in carts%}
        <tr>
            <td>{{cart.id}}</td>
            {%for book in cart.books%}
                <td> <img src="{{asset('Couvertures/'~book.cover)}}" alt="{{book.title}}" class="img-resized2" > </td>
                <td>{{book.title}}</td>
            {%endfor%}
            <td>{{cart.status}}</td>
        </tr>
    {%endfor%}
    </table>
{% endblock %}